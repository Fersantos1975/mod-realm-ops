# RealmOps v0.2.0 — Phases 1–5

Implemented scope:

1. Compact compatibility panel.
2. Central `RealmOpsBuildInfo` service.
3. Instance logic moved from `mod_realm_ops.cpp` to `Inspectors/Instance`.
4. Quest logic moved from `mod_realm_ops.cpp` to `Inspectors/Quest`.
5. Protocol v1 output centralized in `Protocol/LegacyProtocol`.

The environment section remains intentionally deferred.

## Compatibility protocol

Protocol v1 is retained. The VERSION message adds the backward-compatible
`coredirty` field. Older addon builds ignore unknown fields.

## Required validation

This source was prepared from the uploaded v0.1.6 archive. It must be compiled
against the target AzerothCore checkout and tested in game before release.

Recommended checks:

- `.realmops version`
- `.realmops instance search pit`
- `.realmops instance audit 658`
- `.realmops quest search wrath`
- `.realmops quest info <known quest id>`
- Interface Options → RealmOps → Check compatibility
